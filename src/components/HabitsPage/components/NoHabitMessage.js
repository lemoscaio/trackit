import React from "react";
import * as S from "../../../styles/styles"

function NoHabitMessage() {
    return (
        <S.NoHabitMessage>
            <p>
                Você não tem nenhum hábito cadastrado ainda. Adicione um hábito para
                começar a trackear!
            </p>
        </S.NoHabitMessage>
    );
}

export default NoHabitMessage;
